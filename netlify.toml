[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback so React Router routes (e.g. /about) work on refresh / direct links
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
